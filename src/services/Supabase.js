import react from "react";
import { createClient } from "@supabase/supabase-js";


export const supabaseURL = "ksrjgirg;rgn";

const supabaseKey = "";

const supabase = CreateClient(supabaseURL, supabaseKey);

export default supabase;